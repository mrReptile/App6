<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="App6.PageTwo"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">
    <ContentPage.Content>
        <StackLayout>
            <Label
                Margin="50"
                HorizontalOptions="CenterAndExpand"
                Text="Pagina 2" />

            <Button
                Margin="10"
                Command="{Binding GoPageThree}"
                Text="Ver perfil" />
            <Button
                Margin="50"
                Command="{Binding GoRoot}"
                Text="Cerrar sesion" />

        </StackLayout>
    </ContentPage.Content>
</ContentPage>